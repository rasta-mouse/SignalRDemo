<Window x:Class="Client.Views.LoginView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Client.Views"
        mc:Ignorable="d"
        Title="Login" Height="300" Width="500"
        WindowStartupLocation="CenterScreen">
    <Grid>
        <StackPanel Width="200" VerticalAlignment="Center" HorizontalAlignment="Center">
            <TextBlock Text="Set your username:" Margin="5"/>
            <TextBox Text="{Binding Username}" Margin="5"/>
            <Button Content="Login" IsDefault="True" Command="{Binding LoginCommand}" Margin="5"/>
        </StackPanel>
        
    </Grid>
</Window>
